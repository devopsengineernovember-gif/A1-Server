apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mcp-orchestrator-api-pdb
  namespace: a1-orchestrator
  labels:
    app: mcp-orchestrator-api
    component: api
    environment: production
    cluster: a1
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mcp-orchestrator-api