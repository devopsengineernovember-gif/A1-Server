apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp-gateway-netpol
  namespace: a1-orchestrator
  labels:
    app: mcp-gateway
    component: gateway
    environment: production
    cluster: a1
spec:
  podSelector:
    matchLabels:
      app: mcp-gateway
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from: []  # Allow ingress from anywhere
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: a1-orchestrator
    - podSelector:
        matchLabels:
          app: mcp-orchestrator-api
  - to:
    - namespaceSelector:
        matchLabels:
          name: a1-orchestrator
    - podSelector:
        matchLabels:
          app: mcp-policy-proxy
  - to: []  # Allow DNS resolution
    ports:
    - protocol: UDP
      port: 53
  - to: []  # Allow HTTPS egress for external APIs
    ports:
    - protocol: TCP
      port: 443