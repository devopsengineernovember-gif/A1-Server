apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mcp-tracehub-pdb
  namespace: a1-orchestrator
  labels:
    app: mcp-tracehub
    component: tracing
    environment: production
    cluster: a1
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mcp-tracehub