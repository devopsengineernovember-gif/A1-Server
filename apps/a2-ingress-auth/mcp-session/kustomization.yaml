apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: mcp-session

resources:
  - deployment.yaml
  - servicemonitor.yaml
  - networkpolicy.yaml
  - poddisruptionbudget.yaml
  # Temporarily exclude until External Secrets Operator is installed
  # - externalsecret.yaml

commonLabels:
  app.kubernetes.io/component: session-manager
  app.kubernetes.io/part-of: a2-stack

replicas:
  - name: mcp-session
    count: 2

images:
  - name: nginx
    newName: nginx
    newTag: 1.24-alpine